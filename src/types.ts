export type AppState = {
    cartValue: number,
    deliveryDistance: number,
    numberOfItems: number,
    orderTime: number,
}

export type OrderTimeDetails = {
    year: number,
    month: number,
    date: number,
    hour: number,
    minute: number,
}
